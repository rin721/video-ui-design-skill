# Final Cleanup Checklist

## File Tree

- `SKILL.md` exists.
- `README.md` exists.
- `design-system/` exists with required design files.
- `components/` exists with required component files.
- `patterns/` exists with required page files.
- `prompts/` exists with required prompt files.
- `assets/` exists with required media guidance.
- `qa/` exists with required checklists.

## Data

- `design-system/tokens.json` parses as valid JSON.
- Token files separate primitive and semantic values.
- `tokens.css` includes `:root`, `[data-theme="dark"]`, semantic variables, and breakpoint variables.

## Content

- No protected brand identifiers, URLs, class names, images, or distinctive phrases.
- No process notes remain in final artifacts.
- Prompts are standalone and reusable.
- Components include states, accessibility, behavior, and usage rules.
- Page templates support site type, density, content, brand, and conversion changes.

## Quality

- Originality checklist passes.
- Accessibility checklist passes.
- Responsive checklist passes.
- Generated package is ready for website generation, style swapping, component generation, page template generation, brand adaptation, and accessibility review.
